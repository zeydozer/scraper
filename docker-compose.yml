services:
  scraper:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - .:/app
    command: python scraper.py
    restart: "no"

  merge:
    build:
      context: .
    volumes:
      - .:/app
    command: python merge.py
    restart: "no"
